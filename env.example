# Database
# Development (10 connections)
# DATABASE_URL=postgresql://avito_user:password@localhost:5432/avito_db?connection_limit=10&pool_timeout=10

# Production - High Load (recommended)
DATABASE_URL=postgresql://avito_user:password@localhost:5432/avito_db?connection_limit=50&pool_timeout=20&connect_timeout=10&socket_timeout=30

# Connection Pool Parameters:
# - connection_limit: Max connections (10-100, default: 10)
# - pool_timeout: Wait for free connection, seconds (default: 10)
# - connect_timeout: TCP connection timeout, seconds (default: 5)
# - socket_timeout: Query execution timeout, seconds (default: 0 = no timeout)
# 
# Formulas for sizing:
# connection_limit = (CPU cores * 2) + effective_spindle_count
# For 4 core server with SSD: (4 * 2) + 1 = 9, round up to 10-20
# For production with replicas: 50-100 per instance

# JWT Authentication
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Encryption (32+ characters for AES-256)
ENCRYPTION_KEY=change-this-to-a-secure-32-character-key-for-production!!

# Server Configuration
PORT=5004
NODE_ENV=production

# CORS
CORS_ORIGIN=https://callcentre.lead-schem.ru,https://new.lead-schem.ru,https://lead-schem.ru,https://core.lead-schem.ru

# Optional: Logging
LOG_LEVEL=info

