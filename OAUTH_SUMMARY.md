# OAuth Implementation Summary

## ✅ Что сделано

### 1. **OAuth Module** (`src/oauth/`)
- ✅ `oauth.service.ts` - Сервис для работы с OAuth (получение и обновление токенов)
- ✅ `oauth.controller.ts` - Контроллер с эндпоинтами authorize, callback, refresh
- ✅ `oauth.module.ts` - Модуль OAuth
- ✅ `dto/oauth.dto.ts` - DTO для OAuth запросов

### 2. **Updated Services**
- ✅ `avito-api.service.ts` - Убрана авторизация через `client_credentials`, теперь только OAuth токены
- ✅ `app.module.ts` - Подключен `OAuthModule`

### 3. **Configuration**
- ✅ `env.example` - Добавлены переменные для OAuth:
  - `AVITO_OAUTH_CLIENT_ID`
  - `AVITO_OAUTH_CLIENT_SECRET`
  - `AVITO_OAUTH_REDIRECT_URI`
  - `AVITO_OAUTH_SCOPES`

### 4. **Documentation**
- ✅ `README.md` - Обновлена документация с инструкциями по OAuth
- ✅ `OAUTH_MIGRATION.md` - Полное руководство по миграции
- ✅ `OAUTH_QUICKSTART.md` - Быстрый старт за 5 минут
- ✅ `CHANGELOG.md` - Добавлена версия 3.0.0 с описанием изменений

---

## 🎯 Архитектура OAuth

```
┌─────────────────┐
│   Пользователь  │
└────────┬────────┘
         │
         │ 1. Создает аккаунт (без токенов)
         ▼
┌─────────────────────────────────────────┐
│  POST /api/v1/accounts                  │
│  { name, userId, proxy... }             │
│  ➜ Ответ: { id: 1, clientId: null }    │
└────────┬────────────────────────────────┘
         │
         │ 2. Переходит по ссылке OAuth
         ▼
┌─────────────────────────────────────────┐
│  GET /api/v1/oauth/avito/authorize/1    │
│  ➜ Редирект на Avito                    │
└────────┬────────────────────────────────┘
         │
         │ 3. Авторизуется на Avito
         ▼
┌─────────────────────────────────────────┐
│  Avito OAuth Page                       │
│  Пользователь подтверждает доступ      │
└────────┬────────────────────────────────┘
         │
         │ 4. Avito редиректит обратно с code
         ▼
┌─────────────────────────────────────────┐
│  GET /api/v1/oauth/avito/callback       │
│  ?code=XXX&state=YYY                    │
│  ➜ Обмен code на токены                 │
│  ➜ Сохранение в БД                      │
└────────┬────────────────────────────────┘
         │
         │ 5. Редирект на фронтенд
         ▼
┌─────────────────────────────────────────┐
│  https://lead-shem.ru/accounts/1        │
│  ?oauth=success                         │
└─────────────────────────────────────────┘
```

---

## 💾 Хранение токенов

### В БД (таблица `avito_account`):

| Поле | Старое значение | Новое значение (OAuth) |
|------|----------------|------------------------|
| `clientId` | `abc123` (client_id) | `eyJhbGci...` (access_token, ~500+ chars) |
| `clientSecret` | `secret456` (client_secret) | `def50200...` (refresh_token, ~100+ chars) |

### В `.env` (глобально для всех аккаунтов):

```env
AVITO_OAUTH_CLIENT_ID=abc123           # Client ID приложения
AVITO_OAUTH_CLIENT_SECRET=secret456    # Client Secret приложения
```

---

## 🔄 Автоматическое обновление токенов

### Логика в `avito-api.service.ts`:

```typescript
async getAccessToken(): Promise<string> {
  // Возвращаем токен из clientId (это уже access_token)
  return this.clientId;
}
```

### Логика в `oauth.service.ts`:

```typescript
async ensureValidToken(accountId, accessToken, refreshToken): Promise<string> {
  try {
    // Проверяем токен
    await axios.get('/accounts/self', { headers: { Authorization: `Bearer ${accessToken}` }});
    return accessToken; // Токен валиден
  } catch {
    // Токен невалиден, обновляем
    const newTokens = await refreshAccessToken(refreshToken);
    await saveTokensToAccount(accountId, newTokens);
    return newTokens.access_token;
  }
}
```

---

## 🔌 API Endpoints

### OAuth Endpoints

| Method | Endpoint | Описание |
|--------|----------|----------|
| GET | `/api/v1/oauth/avito/authorize/:accountId` | Начать OAuth (редирект на Avito) |
| GET | `/api/v1/oauth/avito/callback` | Callback от Avito (обработка code) |
| GET | `/api/v1/oauth/avito/refresh/:accountId` | Ручное обновление токена |

### Accounts Endpoints (без изменений)

| Method | Endpoint | Описание |
|--------|----------|----------|
| POST | `/api/v1/accounts` | Создать аккаунт (без токенов) |
| GET | `/api/v1/accounts/:id` | Получить аккаунт |
| PUT | `/api/v1/accounts/:id` | Обновить аккаунт |
| DELETE | `/api/v1/accounts/:id` | Удалить аккаунт |
| POST | `/api/v1/accounts/:id/check-connection` | Проверить подключение |

---

## 🌐 Redirect URI

### Что это?

Адрес вашего сервера, куда Avito перенаправит пользователя после авторизации.

### Для разных окружений:

```
Production:  https://api.lead-shem.ru/api/v1/oauth/avito/callback
Staging:     https://api.test-shem.ru/api/v1/oauth/avito/callback
Local:       http://localhost:5004/api/v1/oauth/avito/callback (только для теста)
```

### Регистрация на Avito:

1. Перейти на https://developers.avito.ru/applications
2. Создать приложение
3. Указать Redirect URI: `https://api.lead-shem.ru/api/v1/oauth/avito/callback`
4. Получить CLIENT_ID и CLIENT_SECRET

---

## 🔐 Scopes (права доступа)

### Доступные scopes:

```
messenger:read         - Чтение сообщений
messenger:write        - Отправка сообщений
user:read             - Информация о пользователе
user_balance:read     - Баланс аккаунта
user_operations:read  - История операций
items:info            - Информация об объявлениях
items:apply_vas       - Применение услуг
stats:read            - Статистика
autoload:reports      - Отчеты автозагрузки
job:cv                - Резюме (вертикаль Работа)
job:vacancy           - Вакансии
job:applications      - Отклики
short_term_rent:read  - Краткосрочная аренда (чтение)
short_term_rent:write - Краткосрочная аренда (запись)
```

### Рекомендуемые scopes:

```env
AVITO_OAUTH_SCOPES=messenger:read,messenger:write,user:read,items:info,stats:read,user_balance:read
```

---

## 🧪 Тестирование

### 1. Создание аккаунта

```bash
curl -X POST http://localhost:5004/api/v1/accounts \
  -H "Content-Type: application/json" \
  -d '{"name": "Test Account", "userId": "12345"}'
```

### 2. OAuth авторизация

Откройте в браузере:
```
http://localhost:5004/api/v1/oauth/avito/authorize/1
```

### 3. Проверка подключения

```bash
curl -X POST http://localhost:5004/api/v1/accounts/1/check-connection
```

### 4. Обновление токена

```bash
curl http://localhost:5004/api/v1/oauth/avito/refresh/1
```

---

## 📊 Статус миграции

- ✅ OAuth модуль создан
- ✅ Авторизация через client_credentials удалена
- ✅ Документация обновлена
- ✅ Примеры и гайды созданы
- ⏳ Регистрация приложения на Avito (требуется вручную)
- ⏳ Миграция существующих аккаунтов (требуется вручную)
- ⏳ Обновление фронтенда (если нужно)

---

## 🚀 Следующие шаги

1. **Зарегистрировать приложение** на https://developers.avito.ru/applications
2. **Добавить credentials** в `.env`
3. **Перезапустить сервис**
4. **Протестировать** OAuth flow
5. **Обновить фронтенд** (добавить кнопку "Подключить Avito")
6. **Мигрировать аккаунты** (пройти OAuth для каждого)

---

## 📞 Контакты

- **Документация:** `README.md`, `OAUTH_MIGRATION.md`, `OAUTH_QUICKSTART.md`
- **Swagger:** `https://api.lead-shem.ru/api/docs`
- **Логи:** `kubectl logs -f deployment/avito-service`

---

**Версия:** 3.0.0  
**Дата:** 2025-01-XX  
**Статус:** ✅ Ready for deployment

