# Миграция на OAuth авторизацию Avito

## 🎯 Что изменилось

**Раньше:** Использовали персональные API ключи (`client_credentials`)
**Теперь:** Используем OAuth 2.0 (`authorization_code`)

### Преимущества OAuth:
- ✅ **Бесплатно** (персональные ключи стали платными)
- ✅ Более безопасно (токены обновляются автоматически)
- ✅ Можно работать с несколькими аккаунтами через одно приложение
- ✅ Пользователи контролируют доступ

---

## 📋 Шаг 1: Регистрация приложения на Avito

1. Перейдите на https://developers.avito.ru/applications
2. Нажмите "Создать приложение"
3. Заполните форму:
   - **Имя приложения**: `Lead Schem CRM` (или ваше название)
   - **Redirect URI**: `https://api.lead-shem.ru/api/v1/oauth/avito/callback`
   - **Описание**: `Интеграция с CRM системой для управления объявлениями и мессенджером`
   - **Scopes** (выберите нужные):
     - `messenger:read` - Чтение сообщений
     - `messenger:write` - Отправка сообщений
     - `user:read` - Информация о пользователе
     - `items:info` - Информация об объявлениях
     - `stats:read` - Статистика
     - `user_balance:read` - Баланс аккаунта
     - `autoload:reports` - Отчеты автозагрузки
     - `items:apply_vas` - Применение услуг

4. После регистрации получите:
   - `CLIENT_ID` (например: `abc123def456`)
   - `CLIENT_SECRET` (например: `secret_xyz789`)

**⚠️ Важно:** Avito регистрирует только доверенные приложения от партнеров. Возможно потребуется связаться с поддержкой.

---

## 🔧 Шаг 2: Настройка сервера

### 2.1. Обновите `.env` файл

Добавьте новые переменные окружения:

```env
# Avito OAuth Configuration
AVITO_OAUTH_CLIENT_ID=abc123def456
AVITO_OAUTH_CLIENT_SECRET=secret_xyz789
AVITO_OAUTH_REDIRECT_URI=https://api.lead-shem.ru/api/v1/oauth/avito/callback

# Scopes (можно изменить под свои нужды)
AVITO_OAUTH_SCOPES=messenger:read,messenger:write,user:read,items:info,stats:read,user_balance:read
```

### 2.2. Перезапустите сервис

```bash
# Docker
docker-compose restart avito-service

# Kubernetes
kubectl rollout restart deployment/avito-service

# Local
npm run start:prod
```

---

## 👥 Шаг 3: Миграция существующих аккаунтов

### Вариант А: Создание новых аккаунтов

Если у вас мало аккаунтов, проще создать новые:

1. **Создайте аккаунт без токенов:**

```bash
curl -X POST https://api.lead-shem.ru/api/v1/accounts \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Аккаунт Avito",
    "userId": "12345",
    "eternalOnlineEnabled": true,
    "onlineKeepAliveInterval": 300
  }'
```

**Ответ:**
```json
{
  "id": 1,
  "name": "Аккаунт Avito",
  "clientId": null,
  "clientSecret": null,
  "connectionStatus": "disconnected"
}
```

2. **Авторизуйте через OAuth:**

Откройте в браузере:
```
https://api.lead-shem.ru/api/v1/oauth/avito/authorize/1
```

3. **Подтвердите доступ** на странице Avito

4. **Готово!** Токены сохранены автоматически

### Вариант Б: Обновление существующих аккаунтов

Если у вас уже есть аккаунты со старыми ключами:

1. **Очистите старые ключи:**

```bash
curl -X PUT https://api.lead-shem.ru/api/v1/accounts/1 \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "clientId": null,
    "clientSecret": null
  }'
```

2. **Авторизуйте через OAuth** (см. Вариант А, шаг 2)

---

## 🔄 Шаг 4: Автоматическое обновление токенов

Токены обновляются **автоматически** при запросах к API Avito.

**Срок действия токенов:**
- `access_token` - 24 часа
- `refresh_token` - 1 год

Когда `access_token` истекает, сервис автоматически:
1. Использует `refresh_token` для получения нового `access_token`
2. Сохраняет новые токены в БД
3. Продолжает работу без ошибок

### Ручное обновление (для тестирования)

```bash
curl https://api.lead-shem.ru/api/v1/oauth/avito/refresh/1
```

**Ответ:**
```json
{
  "success": true,
  "message": "Token refreshed successfully",
  "expiresIn": 86400
}
```

---

## 🧪 Шаг 5: Тестирование

### 5.1. Проверка подключения

```bash
curl -X POST https://api.lead-shem.ru/api/v1/accounts/1/check-connection \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

**Ожидаемый результат:**
```json
{
  "connectionStatus": "connected",
  "proxyStatus": "connected",
  "accountInfo": {
    "id": 12345,
    "name": "Иван Иванов",
    "email": "ivan@example.com"
  }
}
```

### 5.2. Проверка мессенджера

```bash
curl https://api.lead-shem.ru/api/v1/messenger/accounts/1/chats \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

### 5.3. Проверка статистики

```bash
curl -X POST https://api.lead-shem.ru/api/v1/accounts/1/sync-stats \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

---

## 🎨 Интеграция с фронтендом

### Кнопка "Подключить Avito"

```javascript
// React пример
function ConnectAvitoButton({ accountId }) {
  const handleConnect = () => {
    // Редирект на OAuth
    window.location.href = `https://api.lead-shem.ru/api/v1/oauth/avito/authorize/${accountId}`;
  };

  return (
    <button onClick={handleConnect}>
      Подключить Avito
    </button>
  );
}
```

### Обработка результата

После авторизации пользователь вернется на:
```
https://lead-schem.ru/accounts/1?oauth=success
```

Или в случае ошибки:
```
https://lead-schem.ru/accounts?oauth=error&message=Error+description
```

```javascript
// Проверка результата
const urlParams = new URLSearchParams(window.location.search);
const oauthStatus = urlParams.get('oauth');

if (oauthStatus === 'success') {
  showNotification('Аккаунт успешно подключен!');
} else if (oauthStatus === 'error') {
  const message = urlParams.get('message');
  showNotification(`Ошибка: ${message}`);
}
```

---

## ❓ FAQ

### Q: Что хранится в `clientId` и `clientSecret`?

**A:** После OAuth:
- `clientId` = `access_token` (JWT токен, ~500+ символов)
- `clientSecret` = `refresh_token` (строка, ~100+ символов)

### Q: Нужно ли удалять старые аккаунты?

**A:** Нет, можно просто обновить их через OAuth. Старые ключи перезапишутся токенами.

### Q: Как часто нужно обновлять токены?

**A:** Автоматически! Сервис сам обновляет токены при необходимости.

### Q: Что делать если токен истек и refresh_token тоже?

**A:** Нужно заново пройти OAuth авторизацию:
```
https://api.lead-shem.ru/api/v1/oauth/avito/authorize/{accountId}
```

### Q: Можно ли использовать старые API ключи?

**A:** Нет, поддержка `client_credentials` полностью удалена. Только OAuth.

### Q: Как работает прокси с OAuth?

**A:** Прокси настраивается для каждого аккаунта отдельно и используется при всех запросах к Avito API, включая получение и обновление токенов.

---

## 🚨 Troubleshooting

### Ошибка: "OAuth configuration missing"

**Причина:** Не заданы переменные окружения

**Решение:**
```bash
# Проверьте .env
echo $AVITO_OAUTH_CLIENT_ID
echo $AVITO_OAUTH_CLIENT_SECRET

# Добавьте если отсутствуют
```

### Ошибка: "Invalid redirect_uri"

**Причина:** Redirect URI в Avito не совпадает с настройками

**Решение:**
1. Проверьте настройки приложения на https://developers.avito.ru/applications
2. Redirect URI должен быть: `https://api.lead-shem.ru/api/v1/oauth/avito/callback`
3. Убедитесь что используется HTTPS

### Ошибка: "Access token not found"

**Причина:** Аккаунт не авторизован через OAuth

**Решение:**
```bash
# Перейдите по ссылке для авторизации
https://api.lead-shem.ru/api/v1/oauth/avito/authorize/{accountId}
```

### Ошибка: "Refresh token expired"

**Причина:** Refresh token истек (через 1 год)

**Решение:** Заново пройти OAuth авторизацию

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи сервиса: `kubectl logs -f deployment/avito-service`
2. Проверьте Swagger документацию: `https://api.lead-shem.ru/api/docs`
3. Проверьте настройки приложения на Avito

---

## ✅ Checklist миграции

- [ ] Зарегистрировано приложение на Avito
- [ ] Получены CLIENT_ID и CLIENT_SECRET
- [ ] Обновлен .env файл
- [ ] Перезапущен сервис
- [ ] Созданы/обновлены аккаунты
- [ ] Пройдена OAuth авторизация для каждого аккаунта
- [ ] Проверено подключение к API
- [ ] Проверена работа мессенджера
- [ ] Обновлен фронтенд (если нужно)
- [ ] Протестирована работа в продакшене

**Готово! 🎉**

